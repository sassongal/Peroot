# [מסמך 10] QA Plan + Test Cases

### סוגי בדיקות
1.  **Unit Tests:** בדיקת פונקציות שירות (למשל: המרת פורמטים).
2.  **E2E (Playwright):** תסריט מלא של משתמש נרשם -> משפר פרומפט -> מעתיק.
3.  **LLM Eval:** בדיקה ידנית/חצי-אוטומטית של איכות הפלטים (האם הפרומפט שנוצר באמת טוב?).

### Test Cases לדוגמה
1.  **TC-01:** הזנת קלט ריק -> המערכת לא שולחת בקשה ומציגה שגיאה.
2.  **TC-02:** הזנת קלט באנגלית -> המערכת מזהה ומחזירה פרומפט משופר (בעברית או אנגלית לפי הגדרה).
3.  **TC-03:** בדיקת RTL במובייל -> הטקסט לא "נשבר" או יוצא מהמסך.
4.  **TC-04:** ניתוק רשת בזמן יצירה -> הצגת הודעת שגיאה מתאימה (לא קריסה).

#### Checklist להשלמה
- [ ] הקמת סביבת Staging.
- [ ] הגדרת סט של "Golden Prompts" (קלטים ידועים) לבדיקת רגרסיה באיכות ה-AI.
